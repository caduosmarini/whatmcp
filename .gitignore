node_modules/
.env
.env.*
!.env.example

# The archive and the API key live in ~/.whatmcp/, never in the repo — but guard
# against a stray WHATMCP_STORE pointing here.
*.db
*.db-wal
*.db-shm
*.sqlite
*.sqlite-wal
*.sqlite-shm
windows-cases/
config.json

.DS_Store
